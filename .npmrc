# NPM configuration for ILoveYou project
# Handles peer dependency conflicts with React Native ecosystem

# Use legacy peer deps resolution to handle React Native package conflicts
legacy-peer-deps=true

# Prefer exact versions to avoid unexpected updates
save-exact=true

# Audit security level (moderate allows minor security issues)
audit-level=moderate